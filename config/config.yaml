# Bybit Grid Trading Bot Configuration

# API設定
api:
  testnet: false  # 本番環境を使用
  
# 取引設定
trading:
  symbol: "BTCUSDT"
  leverage: 2
  position_mode: "MergedSingle"  # 両建てモード: "BothSide" or "MergedSingle"
  
# グリッド戦略設定
grid:
  count: 40  # グリッド数
  range_percent: 0.05  # 価格範囲 (±5%)
  min_range_percent: 0.02  # 最小範囲 (±2%)
  max_range_percent: 0.10  # 最大範囲 (±10%)
  use_dynamic_range: true  # 動的範囲調整を使用
  atr_multiplier: 3.0  # ATRの倍数
  atr_period: 14  # ATR計算期間
  
# 注文設定
order:
  min_profit_percent: 0.003  # 最小利益率 (0.3%)
  order_offset_percent: 0.0001  # 注文価格オフセット (0.01%)
  retry_count: 3  # リトライ回数
  retry_delay: 5  # リトライ間隔（秒）
  
# リスク管理設定
risk:
  max_position_ratio: 0.4  # 最大ポジション比率 (総資金の40%)
  daily_loss_limit: 0.05  # 日次損失限界 (5%)
  stop_loss_percent: 0.10  # ストップロス (10%)
  max_drawdown: 0.15  # 最大ドローダウン (15%)
  daily_profit_target: 0.02  # 日次利益目標 (2%)
  
# 手数料設定
fees:
  maker: 0.0002  # メイカー手数料 (0.02%)
  taker: 0.0055  # テイカー手数料 (0.055%)
  
# ログ設定
logging:
  level: "INFO"  # DEBUG, INFO, WARNING, ERROR, CRITICAL
  console: true
  file: true
  log_dir: "logs"
  trade_history: true
  
# 実行設定
execution:
  check_interval: 60  # メインループ実行間隔（秒）
  grid_update_interval: 3600  # グリッド更新チェック間隔（秒）
  position_check_interval: 30  # ポジションチェック間隔（秒）
  
# 通知設定（オプション）
notification:
  enabled: false
  email: ""
  webhook_url: ""
